# Copy to .env.local and fill in. .env.local is gitignored.
# Used by scripts/release.sh for codesign + notarization.

APPLE_ID="your.apple.id@example.com"
APPLE_TEAM_ID="7FC7ZTYMYU"
APPLE_APP_SPECIFIC_PASSWORD="xxxx-xxxx-xxxx-xxxx"

# Optional: override the GitHub repo for `gh release` (default reads from gh)
# RELEASE_REPO="fabriziosalmi/silicondev"
