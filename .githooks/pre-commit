#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT/backend"

if [[ -f ".venv/bin/activate" ]]; then
  source .venv/bin/activate
fi

python ../scripts/check_version_sync.py
python scripts/check_constraints_sync.py
python scripts/run_pytest_clean.py
